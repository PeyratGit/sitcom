function renderUsers(usersHTML) {
  const usersContainer = document.querySelector(".cards-follows");
  usersContainer.innerHTML = usersHTML;
}
renderUsers('<%= j render 'search/users/list', users: @users, origin: @origin %>');
