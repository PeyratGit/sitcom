<% if current_user.is_following?(@user.id) %>
  <%= link_to "Follow", unfollow_user_path(@user.id), method: :post, remote: true, class: "follow-button"%>
<% else %>
  <%= link_to "Following", follow_user_path(@user.id), method: :post, remote: true, class: "following-button" %>
<% end %>
